PORT=3000

# Local MongoDB (what MongoDB Compass connects to by default)
MONGODB_URI=mongodb://localhost:27017/bigsmoke

# If using MongoDB Atlas (cloud) instead, comment the line above and use:
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/myapidb?retryWrites=true&w=majority
